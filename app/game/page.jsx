import GameSession from '../../components/GameSession';

/** Guided: amber highlight ring on bomb, minimal HUD (strikes). Expert steps: /manual */
export default function GamePage() {
  return <GameSession variant="full" />;
}
